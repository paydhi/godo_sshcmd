# godo_sshcmd

Run a command through ssh interface on any machine. Specify a command that you want to run and hand it over to your mom, so you can run this exact command on your home server that you cannot access.
